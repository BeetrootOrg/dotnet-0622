using ConsoleApp;

using static System.Console;

var list = new LinkedList<int>();
WriteLine(list.Length);
list.Add(1);
WriteLine(list.Length);
list.Add(2);
WriteLine(list.Length);
list.Add(3);
WriteLine(list.Length);
